# ReplyAI Landing (React + plain CSS, per-component stylesheets)

## Run
```
npm install
npm run dev
```
Open http://localhost:8080

## Structure
- Each component has its own CSS file next to it (e.g. `Hero.jsx` + `Hero.css`).
- Global tokens, resets, and shared utilities live in `src/styles/global.css`.
